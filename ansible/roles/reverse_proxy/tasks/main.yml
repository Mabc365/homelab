---
- name: Create Caddy compose directory
  file:
    path: "/opt/homelab/compose/proxy"
    state: directory

- name: Copy Caddyfile
  template:
    src: Caddyfile.j2
    dest: "/opt/homelab/compose/proxy/Caddyfile"

- name: Copy Caddy docker-compose.yml
  template:
    src: docker-compose.yml.j2
    dest: "/opt/homelab/compose/proxy/docker-compose.yml"

- name: Start Caddy service
  community.docker.docker_compose:
    project_src: "/opt/homelab/compose/proxy"
    state: present
